#include <stdio.h>
int main()
{
    int n,arr[n],*p,i;
    printf("len of arr: ");
    scanf("%d",n);
    for(i=0;i<n;i++){
        printf("Enter element %d of array: ",i);
        scanf("%d",&arr[i]);
        
    }
    
}